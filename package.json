{
  "name": "makeup-lecture-system-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "uni-scheduler"
  ],
  "scripts": {
    "dev": "npm --workspace uni-scheduler run dev",
    "build": "npm --workspace uni-scheduler run build",
    "vercel-build": "npm --workspace uni-scheduler run vercel-build",
    "start": "npm --workspace uni-scheduler run start",
    "db:push": "npm --workspace uni-scheduler run db:push",
    "db:seed": "npm --workspace uni-scheduler run db:seed",
    "db:studio": "npm --workspace uni-scheduler run db:studio"
  }
}
