{
  "name": "makeup-lecture-system-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix uni-scheduler run dev",
    "build": "npm --prefix uni-scheduler run build",
    "start": "npm --prefix uni-scheduler run start",
    "db:push": "npm --prefix uni-scheduler run db:push",
    "db:seed": "npm --prefix uni-scheduler run db:seed",
    "db:studio": "npm --prefix uni-scheduler run db:studio"
  }
}
